<!-- Navbar Right Menu -->
<div class="navbar-custom-menu">
    <ul class="nav navbar-nav">
        <!-- User Account Menu -->
        <li class="dropdown user user-menu">
            <!-- Menu Toggle Button -->
            <a href="#" class="dropdown-toggle dropdown-user" data-toggle="dropdown">
                <!-- The user image in the navbar-->
               <img src="{{ asset('bundles/backend/img/avatar_default.png') }}" class="user-image" alt="{{ app.user.username }}">
                <!-- hidden-xs hides the username on small devices so only the image appears. -->
                <span class="hidden-xs">{{ app.user.username }}</span>
            </a>
            <ul class="dropdown-menu">
                <!-- The user image in the menu -->
                <li class="user-header">
                        <img class="img-circle" src="{{ asset('bundles/backend/img/avatar_default.png') }}">
                    <p>
                        {{ app.user.username }}
                    </p>
                </li>

                <!-- Menu Footer-->
                <li class="user-footer">

                    <div class="pull-right">
                        <a href="{{ path('logout') }}" class="btn btn-default btn-flat">{{ 'backend.sign_out'|trans }}</a>
                    </div>
                </li>
            </ul>
        </li>

    </ul>
</div>