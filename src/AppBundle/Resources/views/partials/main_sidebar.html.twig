<section class="sidebar">
    <div class="user-panel">
        <div class="pull-left">
            <img class="img-circle" src="{{ asset('bundles/backend/img/avatar_default_wo_bg.png') }}">
            {# {% if app.user.image %}
                 <img class="img-circle" src="{{ asset(getAvatar(app.user.email)) }}">
                 {% else %}
                     <img class="img-circle" src="{{ asset('bundles/app/img/avatar_default.png') }}">
                 {% endif %}#}
        </div>
        <div class="pull-left info">
            <p>{{ app.user.username }}</p>
            <a href="{{ path('logout') }}"><i class="fa fa-circle text-success"></i>{{ 'backend.online'|trans }}</a>
        </div>
    </div>
    <ul data-active-menu="true" class="sidebar-menu" data-active="{{ active }}">
        <li class="header"></li>
        {% if is_granted("ROLE_ADMIN") %}
            <li id="ticket" class="treeview">
                <a href="{{ ticket_link }}" target="_blank"><i class="fa fa-external-link"></i> <span>{{ 'ticket.title_nav'|trans|title }}</span>
                </a>
            </li>
            <li id="flowwwer" class="treeview">
                <a href="{{ flowww_link }}" target="_blank"><i class="fa fa-external-link"></i> <span>{{ 'flowwwer.title_nav'|trans|title }}</span>
                </a>
            </li>
            <li id="video" class="treeview">
                <a href=""><i class="fa fa-bars"></i> <span>{{ 'video.title_nav'|trans|title }}</span>
                <span class="pull-right-container">
                    <i class="fa fa-angle-left pull-right"></i>
                </span>
                </a>
                <ul class="treeview-menu">
                    <li id="admin">
                        <a id="video-link" href="{{ ticket_link }}" target="_blank"><i class="fa fa-external-link"></i> <span>{{ 'video.title_nav'|trans|title }}</span>
                        </a>
                        <a id="video-list" href=""><i
                                    class="fa fa-circle-o"></i>{{ 'backend.list'|trans~' '~'backend.of'|trans~' '~'video.title_admin_nav'|trans }}
                        </a>
                    </li>
                </ul>
            </li>
            <li id="client" class="treeview">
                <a href=""><i class="fa fa-bars"></i> <span>{{ 'client.title_nav'|trans|title }}</span>
                    <span class="pull-right-container">
                        <i class="fa fa-angle-left pull-right"></i>
                    </span>
                </a>
                <ul class="treeview-menu">
                    <li id="admin">
                        <a id="client-create" href="{{ path('admin_client_list') }}"><i class="fa fa-user"></i> <span>{{ 'client.list'|trans|title }}</span>
                        </a>
                    </li>
                </ul>
            </li>
        {% endif %}
    </ul>
</section>
<script>
    $(function(){
        $('.sidebar-menu').activesidebar();
    });
</script>